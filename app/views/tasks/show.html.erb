<h1>Task DETAIL!!!</h1>
  <h3><%= "Task title: #{@task.title}" %></h3>
    <p><%= "Details: #{@task.details}" %></p>
    <p><%= "Status: #{@task.completed}" %></p>

<%= link_to 'Go to list', tasks_path %>
<%= link_to 'Edit task', edit_path(@task) %>
<%= link_to 'Delete task', delete_path, method: :delete, data: { confirm: "Proceed?" } %>
